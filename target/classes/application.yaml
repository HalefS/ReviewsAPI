spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reviews_api
    username: course3
    password: course3

  jpa:
    show-sql: true
      # DO NOT MODIFY
      # Don't let JPA/Hibernate create the schema
    generate-ddl: false
    hibernate:
      ddl-auto: create
  flyway:
    url: jdbc:mysql://localhost:3306/reviews_api
    user: course3
    password: course3



flyway:
  driver: org.hsqldb.jdbcDriver

